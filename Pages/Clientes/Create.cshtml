@page 
@model Lingzu.Pages.Clientes.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Cliente</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <!-- Formulario para enviar datos mediante el método POST -->
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <!-- Muestra los errores de validación del modelo -->
            <!-- Campo ApellidoP -->
            <div class="form-group">
                <label asp-for="Cliente.ApellidoP" class="control-label"></label>
                <!-- Crea un input para el campo ApellidoP del modelo Cliente -->
                <input asp-for="Cliente.ApellidoP" class="form-control" />
                <!-- Muestra los mensajes de validación para el campo ApellidoP -->
                <span asp-validation-for="Cliente.ApellidoP" class="text-danger"></span>
            </div>

            <!-- Campo ApellidoM -->
            <div class="form-group">
                <label asp-for="Cliente.ApellidoM" class="control-label"></label>
                <input asp-for="Cliente.ApellidoM" class="form-control" />
                <span asp-validation-for="Cliente.ApellidoM" class="text-danger"></span>
            </div>

            <!-- Campo Nombre -->
            <div class="form-group">
                <label asp-for="Cliente.Nombre" class="control-label"></label>
                <input asp-for="Cliente.Nombre" class="form-control" />
                <span asp-validation-for="Cliente.Nombre" class="text-danger"></span>
            </div>

            <!-- Campo Nit -->
            <div class="form-group">
                <label asp-for="Cliente.Nit" class="control-label"></label>
                <input asp-for="Cliente.Nit" class="form-control" />
                <span asp-validation-for="Cliente.Nit" class="text-danger"></span>
            </div>

            <!-- Botón de envío -->
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<!-- Enlace para volver a la lista de clientes -->
<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
